sudo: required
services:
  - docker

script:
  - docker build -t mannydesigns/node .
  - docker images mannydesigns/node

before_deploy:
  - docker login -u <oupisey.it@gmail.com> -p <@Gmail.com123>

deploy:
  privider: script
  script: docker push mannydesigns/node
  on:
    branch: master

