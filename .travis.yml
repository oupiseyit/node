sudo: required
services:
  - docker

script:
  - docker build -t mannydesigns/node .
  - docker images mannydesigns/node

before_deploy:
  - sudo docker login -u oupisey -p @Gmail.com123

deploy:
  provider: script
  skip_cleanup: true
  script: sudo docker push mannydesigns/node
  on:
    branch: master

